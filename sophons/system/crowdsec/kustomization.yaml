namespace: crowdsec
resources:
  - resources.yaml # generated by kustomizer
  - secret.sops.yaml
  - middleware.sops.yaml
  - ../../apps/garage/access-keys/infinity-and-beyond
components:
  - ../../common/litestream
  - ../../common/litestream/templ-config

# patches:
#   - patch: |
#       $patch: delete
#       apiVersion: batch/v1
#       kind: Job
#       metadata:
#         name: crowdsec-capi-register-job
#         namespace: crowdsec
patches:
  - target:
      kind: Deployment
      name: crowdsec-lapi
    patch: |-
      - op: replace
        path: /kind
        value: StatefulSet
      - op: remove
        path: /spec/strategy
