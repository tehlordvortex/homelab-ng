namespace: weed-system
resources:
  - resources.yaml # generated by kustomizer
  - service.yaml
  - cfdtunnel.yaml
  - httproute.yaml
  - middleware.yaml
  - traefikservice.yaml
  # - storageclass.yaml # managed seperately

secretGenerator:
  - name: weed-s3
    files:
      - seaweedfs_s3_config=config/s3.sops.json

configMapGenerator:
  - name: weed-filer
    files:
      - filer.conf=config/filer.json
