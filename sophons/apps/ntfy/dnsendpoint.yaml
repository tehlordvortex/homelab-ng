apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: ntfy
  namespace: ntfy
spec:
  endpoints:
    - dnsName: ntfy.sophons.cloud
      recordType: CNAME
      targets:
        - traefik-sango-star-theta.sophons.cloud
