# yaml-language-server: $schema=https://kubernetesjsonschema.dev/v1.18.1/ingress.json
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ntfy
  namespace: ntfy
  labels:
    app.kubernetes.io/name: ntfy
  annotations:
    gethomepage.dev/description: Push notifications
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Alerting & Notifications
    gethomepage.dev/icon: ntfy.png
    gethomepage.dev/name: Ntfy
    external-dns.alpha.kubernetes.io/target: traefik-sango-star-theta.sophons.cloud
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  rules:
    - host: "ntfy.sophons.cloud"
      http:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: ntfy
                port:
                  number: 80
  tls:
    - hosts:
        - "*.sophons.cloud"
      secretName: ntfy-tls
