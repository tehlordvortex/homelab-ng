namespace: ntfy
resources:
  - ingress.yaml
  - service.yaml
  - namespace.yaml
  - statefulset.yaml
  - secret.sops.yaml

configMapGenerator:
  - name: litestream
    files:
      - config/litestream.yaml
  - name: ntfy
    files:
      - server.yaml=config/server.sops.yaml
