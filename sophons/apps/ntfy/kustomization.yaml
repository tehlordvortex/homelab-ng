namespace: ntfy
resources:
  - service.yaml
  - cfdtunnel.yaml
  - httproute.yaml
  - deployment.yaml
  - secret.sops.yaml

configMapGenerator:
  - name: litestream
    files:
      - config/litestream.yaml
      - config/litestream-legacy.yaml

secretGenerator:
  - name: ntfy-config
    files:
      - server.yaml=config/server.sops.yaml
