namespace: karakeep
resources:
  - service.yaml
  - namespace.yaml
  - secret.sops.yaml
  - ingressroute.yaml
  - persistentvolumeclaim.yaml

configMapGenerator:
  - name: litestream
    options:
      disableNameSuffixHash: true
    files:
      - config/litestream.yaml
