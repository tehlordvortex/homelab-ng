apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: ggrpc-headscale
  namespace: headscale
spec:
  endpoints:
    - dnsName: gghs.vrtx.sh
      recordType: CNAME
      targets:
        - hs.vrtx.sh
