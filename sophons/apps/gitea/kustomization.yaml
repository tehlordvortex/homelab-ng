namespace: gitea
resources:
  - tcproute.yaml
  - httproute.yaml
  - dnsendpoint.yaml
  - statefulset.yaml
  - secret.sops.yaml

configMapGenerator:
  - name: litestream
    options:
      disableNameSuffixHash: true
    files:
      - config/litestream.yaml
      - config/litestream-legacy.yaml
  - name: gitea-runner
    options:
      disableNameSuffixHash: true
    files:
      - config/act_runner.yaml
