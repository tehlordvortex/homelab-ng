namespace: gitea
resources:
  - certificate.yaml
  - dnsendpoint.yaml
  - statefulset.yaml
  - secret.sops.yaml
  - ingressroute.yaml

configMapGenerator:
  - name: litestream
    options:
      disableNameSuffixHash: true
    files:
      - config/litestream.yaml
  - name: gitea-runner
    options:
      disableNameSuffixHash: true
    files:
      - config/act_runner.yaml
