namespace: monitoring-system
resources:
  - resources.yaml # generated by kustomizer
  - prefixed

configMapGenerator:
  - name: tempo-runtime
    options:
      disableNameSuffixHash: true
    files:
      - runtime.yaml=config/tempo.runtime.yaml
