apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: grafana
  namespace: monitoring-system
spec:
  endpoints:
    - dnsName: klstrmntr.sophons.cloud
      recordType: CNAME
      targets:
        - cfd-traefik.sophons.cloud
