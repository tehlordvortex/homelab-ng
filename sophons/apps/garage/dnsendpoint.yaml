apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: s3ui-global
  namespace: garage
spec:
  endpoints:
    - dnsName: s3ui-global.sophons.cloud
      recordType: CNAME
      targets:
        - cfd-traefik.sophons.cloud
